{
  "name": "get-or-tile",
  "version": "0.1.0",
  "description": "an example tile server that gets tiles from a local cache or generates them on the fly",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "clear-cache": "rm -rf local_cache && mkdir local_cache"
  },
  "author": "mapsam",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.218.1",
    "express": "^4.16.3",
    "mapnik": "^3.7.1",
    "mkdirp": "^0.5.1"
  }
}
